import { colorsDark } from '../_utils/colors'

const theme = {
    Button: {
        primary: {
            background: colorsDark.primary,
            border: colorsDark.primary,
            color: '#fff',
        },
    },
}

export default theme
